import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.redhands.jpycalc',
  appName: 'Japan Duty Free Calc',
  webDir: 'dist'
};

export default config;
